# Gradle & JVM memory tuning (for 12GB RAM)
org.gradle.jvmargs=-Xmx6144m -Xshareclasses -Xquickstart -Dfile.encoding=UTF-8
kotlin.daemon.jvm.options=-Xmx3072m

# Use all 4 threads (2 cores × HT)
org.gradle.workers.max=4
org.gradle.parallel=true
org.gradle.configureondemand=true

# Persistent build speed-ups
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Kotlin & Compose performance
kotlin.incremental=true
kotlin.compiler.execution.strategy=daemon
kotlin.compose.compiler.optimize=true

# Disable heavy Compose analysis (reduces CPU load)
android.enableComposeCompilerMetrics=false
android.enableComposeCompilerReports=false

# Android / Modular build optimization
android.useAndroidX=true
android.nonTransitiveRClass=true

# KSP incremental build speed
ksp.incremental=true
ksp.incremental.log=false

# Material 3 Composition Stability Optimization ?
compose.flags.stabilityConfiguration=true
